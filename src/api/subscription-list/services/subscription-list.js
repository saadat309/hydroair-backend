'use strict';

/**
 * subscription-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-list.subscription-list');
